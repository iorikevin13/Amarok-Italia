import React from "react";
import ReactDOM from "react-dom/client";
import AmarokItaliaApp from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AmarokItaliaApp />
  </React.StrictMode>
);
